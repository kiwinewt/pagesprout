<%= page_heading 'Edit Blog' %>

<% enhanced_form_for(@blog, :html => { :class => 'content' }) do |f| %>
  <%= f.standard_fields %>
  <% f.actions do %>
    <%= f.submit "Save" %>
    or <%= link_to 'Cancel', @blog %>
  <% end %>
<% end %>

<%= aside_help_for :blogs %>