<%= f.text_field :title %>

<%= f.text_area :body %>

<%= f.text_field :permalink, :note => 'Only letters, numbers, underscores and hypens are allowed.' %>

<%= f.text_field :keywords, :note => 'Comma-seperated keywords for the page. These are used by Search Engines' %>

<% f.checklist do %>
  <%= f.check_box :home_page, :label => 'Set as home page' %>
<% end %>

<% f.checklist do %>
  <%= f.check_box :enabled, :label => 'Published' %>
<% end %>

<%= f.hidden_field :parent_id %>
