<h1><%=h @page.title %></h1>

<%= @page.body %>

<% if logged_in? %>
  <%= link_to 'Edit', edit_page_path(@page) %> |
  <%= link_to 'Delete', @page, :method => :delete %> |
  <%= link_to 'Back', pages_path %>
<% end %>
