<%= page_heading @post.title %>

<div class="section">
  <div class="element">
    <%= @post.body %>
  </div>
  <div class="element">
    <small><%= @post.created_at.strftime(AppConfig.date_string) %> by <%= @post.user.name %></small>
  </div>
</div>
