<%= page_heading 'Edit Blog' %>

<% enhanced_form_for([@blog, @post], :html => { :class => 'content' }) do |f| %>
  <%= f.standard_fields %>
  <% f.actions do %>
    <%= f.submit "Create" %>
    or <%= link_to 'Delete', [@blog, @post], :method => :delete, :confirm => 'Are you sure?' %>
    or <%= link_to 'Cancel', blog_posts_path %>
  <% end %>
<% end %>

<%= aside_help_for :posts %>